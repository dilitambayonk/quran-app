import { LoaderComponent, LoaderContainer } from "./LoaderStyled"

const Loader = () => {
	return (
		<LoaderContainer>
			<LoaderComponent></LoaderComponent>
		</LoaderContainer>
	)
}

export default Loader
